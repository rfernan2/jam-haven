<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Item Descriptions</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body>

<%- include("partials/header") %> 
<main>
    <h1>Our Delicious Jams</h1>
    <div class="grid-container">
        <% items.forEach(item => { %>
            <div class="item-card">
                <img src="<%= item.image %>" alt="<%= item.title %>">
                <h2><%= item.title %></h2>
                <div class="item-details">
                    <p><strong>Condition:</strong> 
                        <%= item.condition.charAt(0).toUpperCase() + item.condition.slice(1) %></p>
                    <p><strong>Price:</strong> $<%= item.price.toFixed(2) %> | <%= item.totalOffers %> offers</p>
                    <p class="details"><strong>Details:</strong> <%= item.details %></p>
                    <p><strong>Seller:</strong> <%= item.user.firstName %> <%= item.user.lastName %></p>
                    
                </div>

                
                <a href="/items/<%= item.id %>" class="view-btn">View Item</a>


               
            </div>
        <% }); %>
    </div>
</main>

<%- include("partials/footer") %>
</body>
</html>